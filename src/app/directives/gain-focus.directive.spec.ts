import { GainFocusDirective } from './gain-focus.directive';

describe('GainFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new GainFocusDirective();
    expect(directive).toBeTruthy();
  });
});
